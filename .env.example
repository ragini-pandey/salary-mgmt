# Production: a Postgres connection string (e.g. from Neon).
# Local dev with docker-compose: postgres://salary:salary@localhost:5432/salary
DATABASE_URL=postgres://salary:salary@localhost:5432/salary

# Vercel: set in the dashboard, not here. .env.local should mirror this
# file with secret values filled in and is gitignored.
